- if devise_mapping.omniauthable?
  ul.snslogin-list
    - resource_class.omniauth_providers.each do |provider|
      li.snslogin-item = link_to t('.sign_in_with_provider', provider: OmniAuth::Utils.camelize(provider)), omniauth_authorize_path(resource_name, provider), class: 'btn', method: :post
